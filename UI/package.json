{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "",
  "license": "",
  "author": "",
  "scripts": {
    "build": "npm install parcel && parcel build index.html",
    "dev": "parcel index.html --open",
    "start": "npm run build && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
